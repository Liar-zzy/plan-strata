---
{"schema":1,"kind":"plan","id":"P004","revision":"v0.1.0","core":"plans/core/core-v0.3.0.md","core_sha256":"5690f47e59e9d79b222a9e191f24a078583b2b86f5f45368edfd60dc43d7cc8f","ready":true,"tasks":[{"id":"T01","type":"development","depends_on":[]}],"integration_required":false}
---

# P004 — 修复交接验收漏洞，明确默认关闭

## 范围与授权

2026-09-07 用户要求先修复上轮审查确认的问题，并保持并行默认关闭。
本轮修复和复验均在本地，不创建 Issue、分支、commit、push、Release 或全局安装。
沿用未发布的 0.1.0-alpha.2 候选版本，保留 P003 计划、检查及首次试用证据。

## T01 — 接收端基线保护与保留标识

修复本地试用驱动在收集前和终验接收前遗漏 Manager 输入基线核对的问题：受检
文件发生新增、删除或修改时，在写入产物、检查和进度前拒绝接受。仍允许既定
同一结果的重复收集，以及 Manager 在允许范围内更新进度，不引入通用调度器。
对完成阶段保留的受检输入清单提供显式验证，以免终验 check 漏列文件后发生
输入变化却仍显示 verified。该清单不能替代验收范围与真实测试的语义审查。

在协议和只读校验器中保留 integration 标识，拒绝将其用作普通任务 ID，防止
同一检查兼任任务与终验，以及研究解释校验被绕过。正常 T01 等任务保持兼容。

明确并行默认关闭，只有用户明确选择当前迭代/批次后才允许派发；技能自动加载
不等于启用并行。自然语言启用即可，不增加配置开关或内置 Git/Issue 执行器。
用户选择在宿主的可用工具与权限范围内生效，不扩展外部发布或主分支合并授权。

## 验收与停止

先将已复现反例转成红色回归测试，再做最小修正；复跑原有测试和反例变体，检查
受影响的重试、进度更新、合法单 Agent 流程与科研负结果仍可使用。Python 3.10
和 3.14 全量测试通过；更新中英文入口和当前验证说明，保存红/绿结果及源指纹。
本任务包含包、文档和驱动的联合验收，无需重复建立终验任务。未做新 Agent
行为试用或真实研究，不将 P003 的旧技能快照和旧 CI 当作本次实现的验证。
