---
{"schema":1,"kind":"plan","id":"P005","revision":"v0.1.0","core":"plans/core/core-v0.2.0.md","core_sha256":"a8ce742801c915dcc574fbd36b466e79111a739296f4c2e8d9265513ae710f57","ready":true,"tasks":[{"id":"T01","type":"development","depends_on":[]}],"integration_required":false}
---

# P005 — main 基础验收 hotfix

## 范围与授权

基于 main 的 d3b28e3，不合入 dev 的并行交接功能。2026-09-07 用户在主分支
审查后同意推进：修复并验证后提交、推送 main，再将公共修复同步到 dev。
沿用 core-v0.2.0 的轻量、标准库、可安装技能目标；本次明确授权覆盖历史发布
准备阶段的暂缓 push。不开启分支保护、不修改 GitHub 设置、不创建 Release/tag，
不全局安装或运行真实科研实验。保留 alpha.1 标签，以提交和源码指纹识别 hotfix。

## T01 — 保留标识、路径异常与输入范围

回移 integration 保留任务 ID 的检查及回归，防止普通科研任务跳过解释字段、
同一 check 同时满足任务和终验。修复 Python 3.10 路径解析循环链接的未捕获异常；
记录引用、检查对象、fingerprint 和项目根路径失败时均返回 JSON 诊断，不吞掉
无关程序错误。保留合法路径、路径隔离、只读校验与有效科研负结果的行为。

补充协议对本地清单的说明：固定输入成员要显式列入 subjects，清单哈希不代表
成员文件已经重新核对。不增加通用清单解析、调度器、并行模板或宿主权限开关。

## 验收与停止

先把已复现的反例转为红色回归，再做局部修正；Python 3.10 和 3.14 全量测试
通过。记录新源码指纹、真实输出及只读验证结果，更新双语验证入口，保留历史
P001/P002 检查。仅将本轮文件提交到 main，推送后核对远端提交和两版 Python CI。
同步 dev 时使用其独立的当前计划与全量测试，不把 main 的验收冒充 dev 的验收。
