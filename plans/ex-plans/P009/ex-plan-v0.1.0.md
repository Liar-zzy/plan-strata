---
{"schema":2,"kind":"plan","id":"P009","revision":"v0.1.0","core":"plans/core/core-v0.4.0.md","ready":true,"tasks":[{"id":"T01","type":"development","depends_on":[]}],"integration_required":false}
---

# P009 — 无哈希默认流程与 dev 新版

依据 2026-09-10 用户同意删除哈希，并要求“再检查一下，如果没问题就 push 新版本”。
前身 P008 已完成；历史代码及证据已保留于提交 2390eed，本轮不改写旧计划与检查。
单 Agent 完成，不启用可选并行，不创建外部 Issue，不合入 main，不建立 GitHub
Release，不修改全局安装；用户的 .gitignore 修改与原始反馈不纳入提交。

## T01 — 完整迁移、验证与发布

交付 0.2.0-alpha.1：技能入口、模板、协议、校验器、评测脚本、测试和中英文使用
文档统一去除强制哈希。subjects/evidence 使用路径列表；输出明确 structure_only，
以 accepted 表示记录中的接受，而非 verified。schema 1 及混合活跃记录须明确拒绝，
提供保留历史与新迭代迁移路径，不维护两套运行模式。

保留任务/计划绑定、依赖、路径安全、实际验收、有限返修、单一进度维护与默认关闭
并行的约束。不得以手工“检查通过”掩盖内容变化检测被移除；必须写出明确的复核责任。

验收：Python 3.10 和 3.14 全套合约测试、技能格式检查、当前记录结构检查、文档链接
与版本对齐，以及临时目录的安装副本检查。实际覆盖无 Git 单任务闭环、负科研结果、
可选隔离交接/集成、失败返修与旧格式拒绝；脚本化测试不冒充独立 Agent 行为试验。
测试应证明校验器不读产物内容，且内容改变不会被自动检测，但显式重开与实际复测
可以恢复正确验收。保留本轮真实结果，再提交并向 origin/dev 正常 push，核对远端
提交与 CI。仅当安全检查发现阻断问题时暂停发布。

范围：skills/plan-strata、tests、evals 的当前脚本与说明、README、当前版本说明及
P009/core v0.4/CURRENT。历史证据文件不改写；过时指南可加醒目的历史说明。
预算：实现与自检，以及最多两轮成组的正式审核返修；不启动额外 Agent/科研实验，
不扩展为通用调度器。网络仅用于已有安装器验证、git push 和远端/CI 核对。
