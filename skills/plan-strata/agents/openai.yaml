interface:
  display_name: "Plan Strata"
  short_description: "Versioned plans and evidence for development and research"
  default_prompt: "Use $plan-strata to recover the current plan binding, report the next bounded action, and execute it only when the current request or assignment authorizes that action."
